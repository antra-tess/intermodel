[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "intermodel"
version = "0.0.9"
dependencies = [
    "openai",
    "dhall",
    "httpx",
    "banana-dev",
    "anthropic",
    "tiktoken",
    "python-dotenv"
]
